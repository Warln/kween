var home = Vue.extend({
	template: '#homelist'
});
var main = Vue.extend({
	template: '#main'
});
var category = Vue.extend({
	template: '#category'
});
var App = Vue.extend({});
//Vue.component('homelist', home);
//Vue.component('category', category);
//Vue.component('main', main);
